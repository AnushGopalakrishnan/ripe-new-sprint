# Agent Startup

Before making changes or answering project-state questions, read:

`.context/memory.md`

That file is the shared project memory for this workspace. Keep it updated when project goals, route decisions, deployment state, source-of-truth assumptions, major fixes, known risks, or user preferences change in a way that future agents need to know.
