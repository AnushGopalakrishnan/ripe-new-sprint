// This file was generated by Slater.app - Ripe Studios

let paths = window.location.pathname.split('/')


// team-card-hover -> 
{ 
   const pages = ["team"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) {    const link = document.createElement('link'); 
    link.rel = 'stylesheet'; 
    link.href = 'https://slater.app/18806/55054.css?v=104266';
    document.head.appendChild(link); 
 }; 
}

// writings-article -> 
{ 
   const pages = ["detail_writing"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('https://slater.app/18806/55373.js?v=941901'); 
} 
 

// writing -> 
{ 
   const pages = ["horizontal-scroll-test", "writing-new"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) {    const link = document.createElement('link'); 
    link.rel = 'stylesheet'; 
    link.href = 'https://slater.app/18806/55156.css?v=574986';
    document.head.appendChild(link); 
 }; 
}

// case-studies-grid-2 -> 
{ 
   const pages = ["work-copy", "work"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('https://slater.app/18806/55132.js?v=317519'); 
} 
 

// index -> 
import('https://slater.app/18806/54920.js?v=536487')


// home-masonry -> 
{ 
   const pages = [""]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) {    const link = document.createElement('link'); 
    link.rel = 'stylesheet'; 
    link.href = 'https://slater.app/18806/54924.css?v=770100';
    document.head.appendChild(link); 
 }; 
}

// writing-horizontal-scroll -> 
{ 
   const pages = ["writing-new-copy", "writing-new"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('https://slater.app/18806/55155.js?v=208250'); 
} 
 

// case-studies-grid -> 
{ 
   const pages = ["case-studies"]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('https://slater.app/18806/55071.js?v=35887'); 
} 
 

// home-masonry -> 
{ 
   const pages = [""]; 
   const path = paths[paths.length - 1] 
   const itemPath = paths[paths.length - 2] 
   if (pages.includes(path) || pages.includes('detail_' + itemPath) || pages.includes(itemPath + '/item')) import('https://slater.app/18806/54923.js?v=909536'); 
} 
 

